---
alwaysApply: false
---
# Orcho Risk Assessment Rule

1. **Call `@orcho assess_risk` with the following request format:**
   `prompt`": The user's prompt, verbatim,
   `context`: {
      `repo_full_name`: A string obtained by running "python .cursor/scripts/get_repo_name.py" within the terminal,
      `current_file`: The path string of the currently open file in the editor,
      `other_files`: A list of file paths that will be modified by the prompt. Analyze the prompt to determine this
   }
   
2. **Display the Orcho risk assessment risk score and details**

3. **Ask the user if they want to proceed with task execution**
